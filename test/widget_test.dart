import 'package:flutter/widgets.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:things_to_win/app/app.dart';

void main() {
  test('ThingsToWinApp is a widget', () {
    expect(const ThingsToWinApp(), isA<Widget>());
  });
}
