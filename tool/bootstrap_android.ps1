# Run this after installing Flutter SDK.
# It generates platform folders (android) without overwriting your existing lib/ architecture.
flutter create --platforms=android --org com.sina.things_to_win .
flutter pub get
